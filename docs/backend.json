
{
  "entities": {
    "UserAccount": {
      "title": "User Account",
      "description": "Represents a betting client account in the system.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "clientCode": { "type": "string" },
        "name": { "type": "string" },
        "password": { "type": "string" },
        "balance": { "type": "number" },
        "exposure": { "type": "number" },
        "status": { "type": "string", "enum": ["Active", "Suspended"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "role": { "type": "string", "enum": ["Admin", "User"] }
      },
      "required": ["clientCode", "name", "password", "balance", "status"]
    },
    "Bet": {
      "title": "Bet",
      "description": "Represents a bet placed by a user on a match or game.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "market": { "type": "string" },
        "team": { "type": "string" },
        "type": { "type": "string" },
        "price": { "type": "string" },
        "stake": { "type": "number" },
        "status": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "sport": { "type": "string" }
      },
      "required": ["userId", "stake", "timestamp"]
    },
    "Transaction": {
      "title": "Transaction",
      "description": "Records balance additions or deductions.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "amount": { "type": "number" },
        "type": { "type": "string", "enum": ["Credit", "Debit"] },
        "timestamp": { "type": "string", "format": "date-time" },
        "description": { "type": "string" }
      },
      "required": ["userId", "amount", "type", "timestamp"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{clientCode}": {
      "schema": "UserAccount",
      "description": "User profile and balance data, keyed by client code."
    },
    "/bets/{betId}": {
      "schema": "Bet",
      "description": "Records of all bets placed across the platform."
    },
    "/transactions/{transactionId}": {
      "schema": "Transaction",
      "description": "History of financial moves."
    }
  }
}
